"""
DEPRECATED: app.shared.utils has been consolidated into app.shared.toolkit.

Use the canonical imports:
- app.shared.toolkit.formatters (truncate, format_json, format_csv, format_ts)
- app.shared.toolkit.helpers    (safe_dict_get, chunk_text, retry_async)
- app.shared.toolkit.parsers    (parse_json, parse_csv)
"""
