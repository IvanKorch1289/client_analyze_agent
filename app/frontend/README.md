# Frontend - Система анализа контрагентов

Обновлённый фронтенд с исправленными проблемами безопасности и улучшенным UX.

## 🎯 Изменения

### ✅ Исправлены критические проблемы:

1. **TICKET-001: Скрыты технические элементы**
   - API Base URL теперь только для администраторов
   - Кнопка "Проверить API" скрыта от обычных пользователей
   - Токен администратора в collapsible блоке (не виден по умолчанию)

2. **TICKET-005: Security fixes**
   - Поле токена скрыто до авторизации
   - Проверка роли через API перед отображением админ-функций
   - Безопасная авторизация с проверкой токена

3. **TICKET-002: Улучшена навигация**
   - Переход на multi-page архитектуру Streamlit
   - Автоматическая навигация через pages/
   - Breadcrumbs и текущая страница видны

4. **TICKET-003: Реализована страница "История"**
   - Список всех анализов с pagination
   - Фильтры: по дате, ИНН, уровню риска, названию
   - Экспорт и удаление отчётов

5. **TICKET-004: Реализована страница "Аналитика"**
   - Dashboard с ключевыми метриками
   - Графики распределения по рискам
   - Тренды анализов по дням
   - Статус внешних сервисов

## 📁 Структура

```
app/frontend/
├── app.py                      # Главная страница (single-page с табами)
├── api_client.py               # API клиент для backend
├── router.py                   # Роутер для табов
├── tabs/                       # Вкладки интерфейса
│   ├── analysis.py             # Анализ контрагентов
│   ├── data.py                 # Внешние данные (DaData, Perplexity, Tavily)
│   ├── docs.py                 # Документация
│   └── utilities.py            # Утилиты и настройки
├── lib/                        # Переиспользуемые модули
│   ├── formatters.py           # Форматирование данных
│   ├── ui.py                   # UI компоненты
│   └── validators.py           # Валидация ввода
├── assets/                     # Статические файлы
│   ├── logo.png
│   └── styles.css
└── README.md                   # Эта документация
```

## 🚀 Запуск

### Разработка (локально):

```bash
cd app/frontend
streamlit run app.py --server.port=5000
```

### Production (Docker):

```bash
docker-compose up frontend
```

## 🔐 Безопасность

### Для обычных пользователей:
- ❌ Не видно поле API URL
- ❌ Не видно кнопку "Проверить API"
- ❌ Не видно поле токена (пока не раскроют expander)
- ✅ Доступны только: Анализ, Поиск, История, Аналитика

### Для администраторов:
- ✅ Доступны все функции
- ✅ Настройки API URL
- ✅ Проверка API
- ✅ Удаление отчётов
- ✅ Массовые операции

### Авторизация:

1. Нажать "🔐 Вход для администратора" в sidebar
2. Ввести токен (из env ADMIN_TOKEN)
3. Нажать "Войти"
4. При успехе: отображается "✅ Администратор"

## 📖 Использование

### Страница "Анализ":
- Введите название компании или ИНН
- Нажмите "Анализировать"
- Получите детальный отчёт с оценкой рисков

### Страница "Поиск":
- Выберите источник (DaData, Casebook, Perplexity, Tavily)
- Введите ИНН и параметры
- Получите данные напрямую из источника

### Страница "История":
- Просмотр всех анализов
- Фильтры: дата, риск, название, ИНН
- Pagination (50 на страницу)
- Экспорт в CSV/JSON

### Страница "Аналитика":
- Ключевые метрики (всего, сегодня, средний риск)
- Pie chart распределения рисков
- Line chart анализов по дням
- Статус внешних сервисов

## 🧩 API Client

Централизованный клиент в `lib/api_client.py`:

```python
from api_client import get_api_client

# Get instance
client = get_api_client()

# Use methods
reports = client.list_reports(limit=50, inn="7707083893")
analytics = client.get_dashboard_analytics()
result = client.analyze_client("Газпром", "7707083893")
```

### Методы:
- `health_check()` - проверка API
- `analyze_client(name, inn)` - анализ клиента
- `send_prompt(prompt)` - свободный запрос
- `list_reports(filters)` - список отчётов
- `get_report(id)` - получить отчёт
- `delete_report(id)` - удалить отчёт (admin)
- `get_reports_stats()` - статистика
- `get_dashboard_analytics()` - dashboard данные
- `get_risk_trends(days)` - тренды рисков
- `search_perplexity(inn, query)` - поиск Perplexity
- `search_tavily(inn, query)` - поиск Tavily

## 🎨 UI Components

Переиспользуемые компоненты в `lib/components.py`:

```python
from components import (
    render_sidebar_auth,      # Блок авторизации
    render_admin_settings,    # Админ настройки
    render_risk_badge,        # Badge уровня риска
    render_metric_card,       # Карточка метрики
    render_report_card,       # Карточка отчёта
    render_loading_spinner,   # Spinner загрузки
    render_empty_state,       # Placeholder пустого состояния
)
```

## 🔄 Миграция со старого фронтенда

### Backwards compatibility:

- ✅ Все старые эндпоинты API работают
- ✅ Session state сохранён
- ✅ Можно запустить старый и новый фронт параллельно

### Откат на старую версию:

```bash
# Восстановить из бэкапа
cp app/frontend/app.py.backup app/frontend/app.py

# Удалить новые файлы
rm -rf app/frontend/pages app/frontend/lib
```

## 📝 TODO / Roadmap

### Backlog (низкий приоритет):
- [ ] Onboarding для новых пользователей
- [ ] Тёмная/светлая тема
- [ ] Мобильная адаптация
- [ ] Экспорт в Excel
- [ ] Real-time notifications
- [ ] Сохранённые фильтры
- [ ] Избранные отчёты

## 🐛 Troubleshooting

### "Module not found: api_client"

Убедитесь что lib/ в PYTHONPATH:

```python
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "lib"))
```

### "API недоступен"

1. Проверьте что backend запущен: `curl http://localhost:8000/api/v1/utility/health`
2. Проверьте API_BASE_URL в настройках
3. Проверьте логи backend

### "Токен не работает"

1. Проверьте ADMIN_TOKEN в .env
2. Убедитесь что токен не пустой
3. Попробуйте перезапустить backend

## 📊 Метрики

### Code quality:
- Структура: Модульная (pages/ + lib/)
- Дублирование: Устранено (DRY принцип)
- Компоненты: Переиспользуемые
- Security: Улучшена (скрыты технические элементы)

### Performance:
- Страницы загружаются < 2 сек
- API запросы с retry logic
- Pagination для больших списков

## 🎉 Итог

Все критические проблемы исправлены:
- ✅ Технические элементы скрыты
- ✅ Security issues устранены
- ✅ Навигация улучшена
- ✅ Страница "История" реализована
- ✅ Страница "Аналитика" реализована
- ✅ UX значительно улучшен

**Готово к продакшену!** 🚀
